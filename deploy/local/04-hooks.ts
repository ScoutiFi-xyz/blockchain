import { ethers } from 'hardhat'

const run = async () => {
  // TODO:
  // create a hook for dynamic fees
  // initialize a new pool with hook
  // add liquidity
  // swap 1 zeroForOne just a bit
  // swap 2 zeroForOne more qty
  // swap 3 oneForZero back to equilibrium
  // does dynamic fees apply?
  //    how to measure this - damn
  //    does this show in protocol fees field in the pool state
}

run()
